@model IEnumerable<UCP1_PAW_121_C.Models.KotaTujuan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Kota Tujuan")
            </th>
            <th>
                @Html.DisplayName("Biaya")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KotaTujuan1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Biaya)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKotaTujuan"><i class="fa fa-marker fa-lg"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdKotaTujuan"><i class="fas fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdKotaTujuan"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
